# Backend NekoMind - dependencias do MVP (Python 3.11+)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
SQLAlchemy>=2.0
python-multipart>=0.0.9   # upload de chunks (multipart/form-data)
websockets>=12

# Testes
pytest>=8.0
httpx>=0.27               # TestClient do FastAPI

# Captura/serial, modo real e suite completa: ver requirements-{mac,real,test}.txt.
